# Servidor HTTP assíncrono
fastapi>=0.115.0
uvicorn[standard]>=0.30.1

# Variáveis de ambiente
python-dotenv>=1.0.1

# Camoufox — browser Firefox (DOM-first + fallback network)
camoufox[geoip]>=0.4.1

# Persistência local
aiosqlite>=0.20.0

# Utilitários
pydantic>=2.11.0
